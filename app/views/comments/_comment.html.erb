<blockquote>
  <p><%= comment.body %></p>
  <footer><%= image_tag(User.find(comment.user_id).avatar.tiny.url) %>  <%= User.find(comment.user_id).name %>, posted <%= time_ago_in_words(comment.created_at) %> ago.</footer>
</blockquote>
