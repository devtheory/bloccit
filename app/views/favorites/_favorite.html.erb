<% if policy(Favorite.new).create? %>
  <div class="favorite">
    <% if favorite = current_user.favorited(post) %>
      <%= link_to [topic, post, favorite], class: 'btn btn-primary', method: :delete do %>
        <i class="glyphicon glyphicon-star"> </i>&nbsp; Unfavorite
      <% end %>
    <% else %>
      <%= link_to [topic, post, Favorite.new], class: 'btn btn-primary', method: :post do %>
        <i class="glyphicon glyphicon-star-empty"> </i>&nbsp; Favorite
      <% end %>
    <% end %>
  </div>
<% end %>